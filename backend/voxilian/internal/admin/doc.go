// Package admin implements operator operations backing both the
// `voxilian admin` CLI and the WS admin role (spec §10). Every
// mutating operation is logged. Full implementation lands in M11-T3.
package admin
