// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: character_abilities.sql

package gen

import (
	"context"
)

const deleteCharacterSkills = `-- name: DeleteCharacterSkills :exec
DELETE FROM character_skills
WHERE character_id = $1
`

func (q *Queries) DeleteCharacterSkills(ctx context.Context, characterID int64) error {
	_, err := q.db.Exec(ctx, deleteCharacterSkills, characterID)
	return err
}

const deleteCharacterSpells = `-- name: DeleteCharacterSpells :exec
DELETE FROM character_spells
WHERE character_id = $1
`

func (q *Queries) DeleteCharacterSpells(ctx context.Context, characterID int64) error {
	_, err := q.db.Exec(ctx, deleteCharacterSpells, characterID)
	return err
}

const insertCharacterSkill = `-- name: InsertCharacterSkill :exec
INSERT INTO character_skills (
    character_id,
    skill_id,
    ability,
    atrophy_flag
)
VALUES ($1, $2, $3, $4)
`

type InsertCharacterSkillParams struct {
	CharacterID int64 `json:"character_id"`
	SkillID     int32 `json:"skill_id"`
	Ability     int16 `json:"ability"`
	AtrophyFlag bool  `json:"atrophy_flag"`
}

func (q *Queries) InsertCharacterSkill(ctx context.Context, arg InsertCharacterSkillParams) error {
	_, err := q.db.Exec(ctx, insertCharacterSkill,
		arg.CharacterID,
		arg.SkillID,
		arg.Ability,
		arg.AtrophyFlag,
	)
	return err
}

const insertCharacterSpell = `-- name: InsertCharacterSpell :exec
INSERT INTO character_spells (
    character_id,
    spell_id,
    ability,
    atrophy_flag
)
VALUES ($1, $2, $3, $4)
`

type InsertCharacterSpellParams struct {
	CharacterID int64 `json:"character_id"`
	SpellID     int32 `json:"spell_id"`
	Ability     int16 `json:"ability"`
	AtrophyFlag bool  `json:"atrophy_flag"`
}

func (q *Queries) InsertCharacterSpell(ctx context.Context, arg InsertCharacterSpellParams) error {
	_, err := q.db.Exec(ctx, insertCharacterSpell,
		arg.CharacterID,
		arg.SpellID,
		arg.Ability,
		arg.AtrophyFlag,
	)
	return err
}

const listCharacterSkills = `-- name: ListCharacterSkills :many
SELECT character_id, skill_id, ability, atrophy_flag
FROM character_skills
WHERE character_id = $1
ORDER BY skill_id
`

func (q *Queries) ListCharacterSkills(ctx context.Context, characterID int64) ([]CharacterSkill, error) {
	rows, err := q.db.Query(ctx, listCharacterSkills, characterID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CharacterSkill
	for rows.Next() {
		var i CharacterSkill
		if err := rows.Scan(
			&i.CharacterID,
			&i.SkillID,
			&i.Ability,
			&i.AtrophyFlag,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listCharacterSpells = `-- name: ListCharacterSpells :many

SELECT character_id, spell_id, ability, atrophy_flag
FROM character_spells
WHERE character_id = $1
ORDER BY spell_id
`

// M1-T6b: character ability primitives (spec §8).
// Reads plus low-level child-row statements for the later M1-T7a
// snapshot CAS (root CAS → delete children → insert snapshot, all via
// q.WithTx). NO handwritten ability setters here: writing existing
// abilities outside characters.revision CAS is forbidden.
func (q *Queries) ListCharacterSpells(ctx context.Context, characterID int64) ([]CharacterSpell, error) {
	rows, err := q.db.Query(ctx, listCharacterSpells, characterID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []CharacterSpell
	for rows.Next() {
		var i CharacterSpell
		if err := rows.Scan(
			&i.CharacterID,
			&i.SpellID,
			&i.Ability,
			&i.AtrophyFlag,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
