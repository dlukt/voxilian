// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: bank_cas.sql

package gen

import (
	"context"
)

const cASUpdateBankBalance = `-- name: CASUpdateBankBalance :one

UPDATE banks
SET
    balance = $1,
    revision = $2::bigint + 1
WHERE character_id = $3
  AND system = $4
  AND revision = $2::bigint
RETURNING revision
`

type CASUpdateBankBalanceParams struct {
	Balance          int64  `json:"balance"`
	ExpectedRevision int64  `json:"expected_revision"`
	CharacterID      int64  `json:"character_id"`
	System           string `json:"system"`
}

// M1-T7c: bank aggregate CAS statement (spec §8.1).
// ONLY this file may contain UPDATE banks. Single-row CAS needs no
// surrounding transaction; future trade composition uses the private
// seam in store code. No generic bank updater, no DELETE (bank deletion
// semantics are unspecified).
func (q *Queries) CASUpdateBankBalance(ctx context.Context, arg CASUpdateBankBalanceParams) (int64, error) {
	row := q.db.QueryRow(ctx, cASUpdateBankBalance,
		arg.Balance,
		arg.ExpectedRevision,
		arg.CharacterID,
		arg.System,
	)
	var revision int64
	err := row.Scan(&revision)
	return revision, err
}
